{% extends 'base.html.twig' %}

{% form_theme form 'form_table_layout.html.twig' %}

{% block body %}
    <h1>Create Activity</h1>

    {{ form_start(form) }}
        {{ form_widget(form) }}
        <input type="submit" value="Create" />
    {{ form_end(form) }}

    <ul>
        <li>
            <a href="{{ path('team_activity_index') }}">Back to the list</a>
        </li>
    </ul>
{% endblock %}
